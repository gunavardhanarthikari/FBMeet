# Server only — used by /api/token.ts. Never exposed to the browser.
# Never commit real values; keep them in Vercel project env vars / a local .env.
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
LIVEKIT_URL=

# Client — safe to expose (Vite inlines VITE_* vars into the browser bundle).
VITE_LIVEKIT_URL=
